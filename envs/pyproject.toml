[project]
name = "qualifying-lfp-content-ids"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "datalad",
    "datalad-container",
    "dandi",
    "remfile",
    "spikeinterface",
]
